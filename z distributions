from scipy import stats as s
x= float(input("Enter the value"))
z=round(s.norm.cdf(x),5) #calculating the z
print("z value is ",z)
z0=round(s.norm.ppf(z),5) # geeting initial value
print("initial value is ",z0)
