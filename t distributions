from scipy.stats import t
# define probability
p =float(input("enter the probability"))
df = int(input("enter the degree of freedom"))
# retrieve value <= probability
value = t.ppf(p, df)
print(round(value,5))
# confirm with cdf p = t.cdf(value, df) print(p)
